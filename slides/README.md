# slides

Slide decks for the **Claude Architect Foundations** course. Built from `scripts/build-deck.py` against the reference O'Reilly template. The `.pptx` is committed for archival so cohorts can grab the latest cut without a local Python toolchain; rebuild from source with `python scripts/build-deck.py` (PowerShell 7+).
