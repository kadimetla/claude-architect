# Project Instructions

## Overview
This project is a [describe your application].

## Coding Standards
- Use TypeScript strict mode for all new files
- All functions must have JSDoc comments
- Error handling: custom error classes, never throw raw strings

## Testing Conventions
- Test files beside code: `Button.test.tsx` next to `Button.tsx`
- Use vitest for unit tests, playwright for E2E
- Mock external services, never internal modules

## What Claude Code Should NOT Do
- Never modify files in `config/production/`
- Never commit secrets or credentials
- Ask before deleting files

@import docs/api-standards.md
